{
  "name": "cucumber",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare:test": "ts-node scripts/prepare.ts",
    "test:chromium": "cross-env BROWSER=chromium TAGS=${TAGS:-} cucumber-js --profile chromium --tags \"$TAGS\"",
    "test:firefox": "cross-env BROWSER=firefox TAGS=${TAGS:-} cucumber-js --profile firefox --tags \"$TAGS\"",
    "test:webkit": "cross-env BROWSER=webkit TAGS=${TAGS:-} cucumber-js --profile webkit --tags \"$TAGS\"",
    "test:all": "ts-node run-tests.ts",
    "report": "ts-node generate-report.ts",
    "ci": "npm run prepare:test && npm run test:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cucumber/cucumber": "^11.3.0",
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.15.21",
    "cross-env": "^7.0.3",
    "multiple-cucumber-html-reporter": "^3.9.2",
    "npm-run-all": "^4.1.5",
    "ts-node": "^10.9.2"
  }
}
